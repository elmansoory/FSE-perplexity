# World Champion Coach Pro v3

Arabic/English figure-skating coaching platform.

## Windows
1. Run `install.bat` once.
2. Run `run.bat`.

Or use:
```powershell
python -m pip install -r requirements.txt
python -m streamlit run app.py
```

This is a coaching/training estimation tool, not an official ISU scoring system.
